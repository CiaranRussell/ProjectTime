@model IEnumerable<ProjectUser>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Assigned Projects to Users List</h2>
        </div>
        <div class="col-6 text-end">
           <a asp-controller="ProjectUser" asp-action="Create" class="btn btn-primary">
               <i class="bi bi-plus-circle"></i> &nbsp; Assign Project to User
           </a>
        </div>
   </div>
   <br />

<table id="tblDataprojectUsers" class="table table-bordered table-striped" style`="width 100%">  
    <thead> 
        <tr>
            <th>Project Name</th>
            <th>User Name</th>
            <th>Is Active</th>
            <th></th>
        </tr>
    </thead>
    @*<tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td width="50%">@obj.Project</td>
                <td width="30%">@obj.FullName</td>
                <td width="30%">@obj.IsActive</td>
                <td>
                    <div class="w-45 btn-group" role="group">
                        <a asp-controller="ProjectUser" asp-action="Edit" asp-route-id="@obj.Id"
                        class="btn btn-primary"><i class="bi bi-pencil-square"></i>Edit</a>
                    </div>
                    <div class="w-45 btn-group" role="group">
                        <a asp-controller="ProjectUser" asp-action="Delete" asp-route-id="@obj.Id"
                        class="btn btn-danger"><i class="bi bi-trash3-fill"></i>Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>*@
</table>
</div> 

@section scripts{
    <script src="~/js/projectUser.js"></script>
    <script src="//cdn.jsdelivr.net/npm/alertifyjs@1.11.2/build/alertify.min.js"></script>
    <script type="text/javascript">
        $(function(){
            var save = '@TempData["save"]'
            if(save!='') {
                alertify.success(save);
            }
            var edit = '@TempData["edit"]'
            if(edit!='') {
                alertify.success(edit);
            }
            var del = '@TempData["delete"]'
            if (del!='') {
                alertify.error(del);
            }
        })
    </script>
}